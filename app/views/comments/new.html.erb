<%= render 'shared/trips_header', trip: nil, title: 'New comment', navbar: false %>

<%= render 'shared/back_button', target: @comment.commentable %>
<%= render 'shared/confirm_button', form: 'comment-form' %>

<%= render 'form', comment: @comment %>
