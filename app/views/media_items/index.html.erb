<h2 class="display-2">Media gallery</h2>

<%= render 'shared/add_button', target: new_media_item_path, resource: 'media item' %>

<div class="row row-cols-1 g-4">
  <%= render @media_items %>
</div>

<% if @pagy.pages > 1%>
  <div class="my-5 text-center">
    <%== pagy_bootstrap_nav @pagy %>
  </div>
<% end %>
