<%= render 'shared/trips_header', trip: @trip, title: 'Media gallery', active: 'media_items' %>

<% if can? :new_trip, MediaItem %>
  <%= render 'shared/add_button', target: trip_new_media_item_path(@trip), resource: 'media item' %>
<% end %>

<div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
  <%= render @media_items %>
</div>
