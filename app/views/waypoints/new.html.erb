<%= render 'shared/trips_header', trip: @trip, title: 'Add waypoint', navbar: false %>

<%= render 'shared/back_button', target: trip_waypoints_path(@trip) %>
<%= render 'shared/confirm_button', form: 'waypoint-form' %>

<%= render 'form', waypoint: @waypoint %>
