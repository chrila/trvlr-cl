<h2 id="comments" class="display-6 mt-5 mb-4">Comments</h2>

<% if can_comment %>
  <%= render 'shared/add_button', target: new_comment_path(commentable), resource: 'comment' %>
<% end %>

<div class="row row-cols-1 row-cols-xl-2 g-3 my-3">
  <%= render commentable.comments.order(:id).reverse %>
</div>
