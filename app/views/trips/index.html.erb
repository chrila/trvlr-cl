<h1 class="display-2">My trips</h1>

<%= render 'shared/add_button', target: new_trip_path, resource: 'trip' %>

<div class="row row-cols-1 g-4">
  <%= render @trips %>
</div>

<% if @pagy.pages > 1%>
  <div class="my-5 text-center">
    <%== pagy_bootstrap_nav @pagy %>
  </div>
<% end %>
