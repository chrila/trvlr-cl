<div class="card my-3">
  <h3 class="display-6"><%= trip.title %></h3>
  <p class="text-muted"><%= "From #{trip.start_date} to #{trip.end_date}, Likes: #{trip.likes_count}" %></p>
  <p><%= trip.description %></p>
</div>